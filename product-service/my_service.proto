syntax = "proto3";

service ArrayService {
  rpc ProcessArray(ArrayRequest) returns (ArrayResponse);
}

message ArrayRequest {
  repeated int32 numbers = 1;
}

message ArrayResponse {
  string message = 1;
}