import { CalendarPipePipe } from './calendar-pipe.pipe';

describe('CalendarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
