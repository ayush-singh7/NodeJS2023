import { ValueFilterPipePipe } from './value-filter-pipe.pipe';

describe('ValueFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValueFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
